# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Env files (keep the example)
.env
.env.*
!.env.example

# Claude Code worktrees (contain a full repo copy + node_modules)
.claude/

# Private notes (kept local, never published)
NOTES.md

# Never track a bun lockfile — this project uses npm.
bun.lock

# OS / editor noise
.DS_Store
*.log